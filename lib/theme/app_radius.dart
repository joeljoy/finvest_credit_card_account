import 'package:flutter/material.dart';

class AppRadius {
  AppRadius._();
  static const none = Radius.zero;
  static const xsmall = Radius.circular(8);
  static const small = Radius.circular(9);
  static const medium = Radius.circular(16);
}